# action.yml
name: 'composer update action'
description: 'composer update and create pull request'
author: 'kai0310'
runs:
  using: 'docker'
  image: 'Dockerfile'
branding:
  icon: 'git-pull-request'
  color: 'gray-dark'
